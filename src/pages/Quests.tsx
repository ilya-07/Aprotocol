import React from "react";

type Props = {};

const Quests = (props: Props) => {
  return (
    <div className="border-t border-l border-[#1b1e28] flex-grow">Quests</div>
  );
};

export default Quests;
